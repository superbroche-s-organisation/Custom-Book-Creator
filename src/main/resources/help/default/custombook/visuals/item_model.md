# Item model

Uses the same model selector presentation as MCreator’s Item element. **Normal** uses the 2D texture. JSON and OBJ models already imported into workspace 3D resources appear with their MCreator icon.
